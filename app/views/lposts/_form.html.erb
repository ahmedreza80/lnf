<%= form_for(lpost) do |f| %>
  <% if lpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lpost.errors.count, "error") %> prohibited this lpost from being saved:</h2>

      <ul>
      <% lpost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tittle %>
    <%= f.text_field :tittle %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :location %>
    <%= f.text_field :location %>
  </div>

  <div class="field">
    <%= f.label :Contact %>
    <%= f.text_field :Contact %>
  </div>

  <div class="field">
    <%= f.label :cover %>
    <%= f.file_field :cover %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
